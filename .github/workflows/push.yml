name: Push
on: push
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - run: |
          systemd-analyze cat-config systemd/resolved.conf
          ls /etc/systemd/network/*.network | while read f; do echo $f; cat $f; done
