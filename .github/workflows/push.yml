name: Push
on: push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $USER
          id -gn
          touch foo
          sudo chown root:root foo
          ls -l foo
          sudo chown $USER: foo
          ls -l foo
