name: Push
on: push
jobs:
  issue:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::warning::There is an issue!"
  check:
    runs-on: ubuntu-latest
    needs: issue
    steps:
      - run: |
          gh run view ${{ github.run_id }}
