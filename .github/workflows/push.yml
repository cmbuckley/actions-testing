name: Push
on: push
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - run: |
          export KV_NAMESPACE_ID=$(curl -X GET "https://httpbin.org/headers" -H "accept: application/json")
          echo "KV: [$KV_NAMESPACE_ID]"
