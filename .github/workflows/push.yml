name: Push
on: push
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - run: |
          pwd
  run_tests:
    name: Run Tests
    runs-on: ubuntu-latest
    steps: 
      - run: echo ${{ github.job }}
